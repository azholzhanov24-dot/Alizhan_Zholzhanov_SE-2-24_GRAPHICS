The program was completely refactored using object-oriented programming (OOP). The entire interface and logic are now encapsulated within the CalculatorApp class, eliminating unsafe global variables. The code is strictly separated according to the single-responsibility principle: interface element creation, data validation, and mathematical calculations are separated into separate, isolated methods. This structure makes the application professional, easy to read, and ready for future scaling.