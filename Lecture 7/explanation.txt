The program demonstrates working with Radiobutton group radio buttons. The radio buttons are linked using the shared variable tk.IntVar(), which controls the user's 
chosen calculation process. The handler function reads the value from the text field, performs standard validation for an empty string and data type, and then uses an 
if-elif conditional statement to determine the selected operation (square, cube, or multiplication by 10) and updates the interface.
